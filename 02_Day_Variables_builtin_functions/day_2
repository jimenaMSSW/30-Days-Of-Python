import math
# day 2 of python programming
first_name, last_name, full_name = 'jane', 'doe', 'jane doe'
country = 'united states'
city = 'new york' 
age = 25
year = 1999
is_married = True
is_true = True
is_light_on = is_true
  
#Exercises: Level 2
print(len(first_name))
print(len(last_name))
print(len(full_name))
print(len(country))
print(len(city))

def compare_names(first_name, last_name):
    if len(first_name) > len(last_name):
        return true
    else:
        return false


num_one = 5 
num_two = 4
total = num_one + num_two
diff = num_one - num_two
product = num_two * num_one
division = num_one/num_two
remainder = num_two % num_one
pow(num_one, num_two)
floor_division = num_one//num_two
area_of_circle = math.pi * (30**2)
circum_of_circle = 2*math.pi*30

user_radius = input('Enter the radius:')
area = math.pi * (pow(float(user_radius), 2))

f_n = input('Enter first name:')
l_n = input('Enter last name:')
ctry = input('Enter country:')
age = input('Enter age:')

print(f_n)
print(l_n)
print(ctry)
print(age)
print(f_n)
